# provider "aws" {
#   region = "us-east-1" #if you want any other region data just mention it will fetch
# }

# data "aws_instance" "example" {
  
# }
# #data source is used to read or fetch information that already exists outside Terraform,

# # resource → creates or manages infrastructure

# # data source → looks up existing infrastructure and uses its information